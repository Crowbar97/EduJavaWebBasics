<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <!-- TODO: fill this fields -->
        <property name="connection.driver_class">driver</property>
        <property name="connection.url">url</property>
        <property name="connection.username">username</property>
        <property name="connection.password">password</property>

        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">1</property>

        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MariaDB53Dialect</property>

        <!-- Echo the SQL to stdout -->
        <!--<property name="show_sql">true</property>-->

		<!-- Set the current session context -->
		<property name="current_session_context_class">thread</property>

        <!-- Mapping classes-->
        <mapping class="edu.own.garage.entities.Mark"/>
        <mapping class="edu.own.garage.entities.Model"/>
        <mapping class="edu.own.garage.entities.OwnModel"/>
    </session-factory>
</hibernate-configuration>